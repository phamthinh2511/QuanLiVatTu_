<UserControl x:Class="PageNavigation.View.KhachHang"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:PageNavigation.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             Style="{StaticResource Page_Style}">
    <UserControl.DataContext>
        <vm:DanhSachKhachHangVM/>
    </UserControl.DataContext>

    
    <Grid Background="#212529">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="590"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock Style="{StaticResource Font_Style}"
                       FontSize="50"
                       Text="Khách Hàng"
                       Margin="20, 0, 0, 0"/>
        </Grid>

        <Grid Grid.Row="1">
            <ListView x:Name="CustomerListView" 
                      Grid.Row="1" 
                      Margin="10" 
                      ItemsSource="{Binding ListCustomers}"  
                      MouseDoubleClick="CustomerListView_MouseDoubleClick">

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding CustomerID}" Width="50"/>
                        <GridViewColumn Header="Tên" DisplayMemberBinding="{Binding CustomerName}" Width="200"/>
                        <GridViewColumn Header="SĐT" DisplayMemberBinding="{Binding CustomerContact}" Width="120"/>
                        <GridViewColumn Header="Giới tính" DisplayMemberBinding="{Binding CustomerGender}" Width="80"/>
                        <GridViewColumn Header="Ngày Sinh" DisplayMemberBinding="{Binding CustomerBirth}" Width="180"/>
                    </GridView>
                </ListView.View>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Style.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="LightBlue"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="LightGreen"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Grid>

    </Grid>
</UserControl>

